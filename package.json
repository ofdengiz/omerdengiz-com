{
  "name": "omerdengiz-com",
  "type": "module",
  "version": "2.0.0",
  "private": true,
  "description": "omerdengiz.com — static portfolio on AWS S3 + CloudFront + Lambda@Edge, provisioned with Terraform",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && node scripts/verify-csp.mjs",
    "build:only": "astro build",
    "preview": "astro preview",
    "check": "astro check",
    "verify:csp": "node scripts/verify-csp.mjs",
    "astro": "astro"
  },
  "dependencies": {
    "@fontsource-variable/ibm-plex-sans": "^5.3.0",
    "@fontsource-variable/space-grotesk": "^5.3.0",
    "@fontsource/ibm-plex-mono": "^5.3.0",
    "astro": "^7.2.4",
    "zod": "^4.4.3"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.10",
    "typescript": "^6.0.3"
  }
}
